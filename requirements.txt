streamlit>=1.35.0

# LangChain + LangGraph multi-agent pipeline
langgraph>=0.2.0
langchain>=0.2.0
langchain-core>=0.2.0
langchain-google-genai>=1.0.0
langchain-community>=0.2.0
google-generativeai>=0.5.0

# Structured outputs
pydantic>=2.0.0

# RAG — vector store + embeddings
chromadb>=0.5.0

# Observability + evals
langsmith>=0.1.0

# PDF generation + parsing
reportlab>=4.0.0
pillow>=10.0.0
pdfplumber>=0.10.0
pymupdf>=1.23.0

# Env
python-dotenv>=1.0.0

# Auth (custom login / signup gate — bcrypt-hashed passwords)
bcrypt>=4.0.0

# RAG cosine similarity (resume chunks)
numpy>=1.24.0
